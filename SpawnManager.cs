using UnityEngine;

public class SpawnManager : MonoBehaviour
{
    [Header("Object Pooler")] [SerializeField]
    private ObjectPooler targetPooler;

    // [Header("Spawn Option")] public int M { get; set; }
}